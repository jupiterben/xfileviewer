# 2026-09-24

## 前端 UX 优化（未改类名/DOM 结构，207 测试全过）
- styles.css 重写：引入 :root 设计令牌（surface/border/text/accent/radius/shadow/easing），全局 focus-visible 焦点环、细圆角滚动条（::-webkit + scrollbar-width 双写，WebView2 走 webkit 伪元素）、prefers-reduced-motion 兜底。
- 交互打磨：sidebar 计数变徽章、active 项加 accent 内嵌条；导航箭头加大命中区（48×96）+ 毛玻璃 + hover 缩放；视频控制条 blur(12px) + 顶部分隔线；播放键 accent 强调；seek 滑杆自定义 4px 轨道 + 进度填充（--seek-percent，playerShell.ts render()/input 两处更新）；音量弹层/弹窗/扫描状态加阴影+blur；dialog 加 dialog-in 入场动画；空状态加 SVG 图标（EmptyState.svelte 新增 .empty-icon）。
- model.css 全量 token 化（保留 fallback 值），工具栏按钮/复选框/信息面板对齐全局风格；markdown 主题切换按钮与选择器加阴影/blur/hover。
- 图片加载揭示：.image-pending 由 visibility:hidden 改为 opacity:0 + .image 200ms 过渡，窗口尺寸就绪后淡入。
- 验证：npm test 207/207、svelte-check 0 错误、vite build 通过（chunk>500kB 警告为既有情况）。
